20231103
■for(int i=0;i<2;i++)
{
for(int j=0;i<3;j++){
}
	//printf("hello"); <- 대신 for 문을 다시 쓴다
}
i=0 j=0 j=1 j=2 안쪽 for문 실행
i=1 
■while 초기값은 바깥으로 뺴고 증감식은 문장밑에 쓰면됨
예시: for(int i=0;i<5;i++)
int i=0;
while(i<5)
{
printf("반복할문장들");
i++;
}
■while(1) 무한루프
"반복할 문장"
break; -> 빠져나온다
■break문 - for,while,do~while 과 같은 반복문을 탈출할떄 사용 
■continue문 - 
■return 문 - 리턴밑에있는 소스는 한번도 실행되지 않는다. - 현재 함수로 호출한 곳으로 돌린다
