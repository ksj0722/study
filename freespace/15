gpu-tpu


딥러닝으로 c++ 만든다
TX1,TX2 

PID 제어  - 모터가 회전할떄 일정하게 회전하게 도와주는거

★python
- highleve & langauge
interpret(인터프리터)
compile(컴파일러)

ipynb ->아이파이썬노트북(jupyter)
파이참은 파이썬
vscode는 쓰면 원리를 잘이해할수있음

아나콘다 - 가상환경을 쓸수있게해줌 콘다 크리에이트 



=================================
환경변수는 한마디로 경로이다. (쉽게설명)
실행할수있는 경로를 지정해준다.
cmd 에서 실행한다.

iDE = 통합계발환경 

===================
파이썬은 무조건 .py로 저장해야 읽어들일수 있다.
python 뱀두마리
튜플은 안에 내용 못바꾸고 (튜플은 바뀌지않는값)
리스트는 바꿀수있다. (리스트가 많이 쓰인다.)

python -> test.py -> test.txt
