하이>로우>기계어
티오베, pypl
c/c++ 개발환경 설정
1. VisualStudio Community <= 무료
└ Microsoft 계정이 있어야함
2. MinGW + VisualStudio Code(VS code)
3. MinGW + CLion(유료버전)

MinGW + VisualStudio Code (편리하기는 하나 초보자들이 접근하기 어렵다)
└ java / python / c / 다른언어 등등

VisualStudio - 프로그램 개발 ( c/c++/c#/VisualBasic)<= 유료버전
_CRT_SECURE_NO_WARNINGS
#Pramga warning(disable:4996)

win + r => regedit => HKEY_LOCAL_MACHINE => SYSTEM => CurrentControlSet=>
Services => LanmanWorks => parameters =>allowlnsecureg => 값을 1로 바꾼후 접속
=>\\192.168.50.94 => share

vm ware 다운

https://code.visualstudio.com/docs/?dv=win (vscode 다운주소)

(다운방법)
vscode -> Extensions -> c/c++ extension pack -> install
(실행후)
Terminal -> configure DefaUlt build task -> 컨트롤 +시프트 + p ->

(mingw)
->실행후 -> architecture ->x86_64 ->

https://sourceforge.net/projects/mingw/ 
-> mingw-developer-toolkit > mingw32-base > mingw32-gcc-g++ > msys-base

내컴퓨터 -> 속성 -> 고급시스템 설정> 편집 

소스파일: 내가 직접 코드입력하는거 (코드 짜는곳)
리소스파일:
헤더파일:   .h붙는소스파일  무조건 헤더파일 <stdio.h>[함수 원형 정의해놓는곳][머리글같은느낌]

#include <stdio.h> 기본적으로 가져오기
└<stdio.h> = standard input output

void main() -> 리턴타입 함수이름(매개변수1,......)

c언어 장점: 빠르다 그렇기떄문에 지금도 c언어가 살아있다.
└자바는  돌아가기 위해 jvm 기본적으로 깐다 
└c는 그냥 돌아간다

컴파일:컴퓨터가 알아먹을수있게 언어번역
컴파일을 하는 도구를 컴파일러
c언어는 컴파일러(파일전체)다 자바는 인터프리터(한줄씩)

ide -> 통합계발툴( 자동화 )

gcc -o 실행파일이름 소스파일이름
gcc -o hello.exe hello2.c
hello.exe

#include <stdlib.h> -> 콘솔창 옵션 -> 1)도구 2)옵션 3)디버깅 4) 일반 
5)디버깅이 중지되면 자동으로... 체크하기

(설정)ctrl + k + c // (해제)ctrl + k + u

https://www.codingfont.com/

확장자 이름은 반드시 .c로 저장합니다.
1).exe - 실행파일  
2)c - .c  c++ - .cpp ,java  - java ,pythan - .py
3)
4).jpg , .png , .gif, .bmp

파일 실행 단축키 
ctrl + F5 
